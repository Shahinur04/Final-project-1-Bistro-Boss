import React from 'react';

const MenuItem = ({item}) => {
    // const {image,price,recipe,category}=item;
    return (
        <div>
            {/* <img src={item.image}  /> */}
        </div>
    );
};

export default MenuItem;